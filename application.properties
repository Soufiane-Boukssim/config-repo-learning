global.params.p1=456
global.params.p2=234

spring.h2.console.enabled=true
spring.cloud.discovery.enabled=true
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
management.endpoints.web.exposure.include=health,refresh

 # Pour s'enregistrer par son address ip a la place de son nom
 #eureka.instance.prefer-ip-address=true
 # Pour quil marche dans nouveaux versions de Spring Cloud
 #eureka.instance.instance-id=${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}

# Force l'ID de l'instance à utiliser localhost
eureka.instance.instance-id=localhost:${spring.application.name}:${server.port}
